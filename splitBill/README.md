# splitBill

一个面向用餐场景的分账工具（单页网页 + 移动端自适应）。

## 开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
npm run preview
```

## 部署到 GitHub Pages

- 本项目使用相对路径资源（`vite.config.ts` 中 `base: "./"`），可直接部署 `dist` 目录。
- 你可以使用 GitHub Actions 或 `gh-pages` 发布。
